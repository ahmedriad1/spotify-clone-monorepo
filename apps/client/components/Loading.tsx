import { LoadingIcon } from '@spotify-clone-monorepo/ui';

const Loading = () => (
  <div className="w-full h-full flex justify-center items-center">
    <LoadingIcon className="w-20 h-20" />
  </div>
);

export default Loading;
